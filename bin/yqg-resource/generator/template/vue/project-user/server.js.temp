/**
 * @author <%= username %>
 * @email <%= email %>
 * @date <%= timeCreated %>
 * @desc generated by <%= signature %>
 */

import config from 'config';

import createSPA from 'yqg-common/server/create-spa';
import runServer from 'yqg-common/server/run-server';

const {port, apiHost} = config.get('<%= hyphenName %>');
const app = createSPA({apiHost});
app.set('port', port);

runServer(app, {port});
