/**
 * @author <%= username %>
 * @email <%= email %>
 * @date <%= timeCreated %>
 * @desc generated by <%= signature %>
 */

import Index from '../../app/index';

export default {
    notFound: {
        path: '*',
        redirect: Index
    },

    root: {
        path: '/',
        redirect: '/index'
    },

    index: {
        path: '/index',
        component: Index
    }
};
