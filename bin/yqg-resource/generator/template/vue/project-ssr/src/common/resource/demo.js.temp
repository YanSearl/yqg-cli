/**
 * @author <%= username %>
 * @email <%= email %>
 * @date <%= timeCreated %>
 * @desc generated by <%= signature %>
 */

export const fetchItem = id => new Promise((resolve) => {
    console.log('fetching item', id); // eslint-disable-line no-console
    setTimeout(() => resolve({id, timestamp: Date.now()}), 100);
});
